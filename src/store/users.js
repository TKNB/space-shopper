import axios from 'axios';

const initialState = {
  users: []
}

// ACTION TYPES


// ACTION CREATORS


// THUNK CREATORS


// REDUCERS
const usersReducer = (users = initialState.users, action) => {
      return users
}

export default usersReducer;
